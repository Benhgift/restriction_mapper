This is a program to compute a simple restriction map for a genome.

run ./res_mapper.py -h for usage

Just feed it all the possible lengths between cuts in your genome, and it'll compute the places where it was cut.

example:
  ./res_mapper.py 2 2 3 3 4 5 6 7 8 10
